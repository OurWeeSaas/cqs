# frozen_string_literal: true

module Command
end

module Query
end

Rails.autoloaders.main.push_dir(
  "#{Rails.root}/app/command", namespace: Command
)

Rails.autoloaders.main.push_dir(
  "#{Rails.root}/app/query", namespace: Query
)