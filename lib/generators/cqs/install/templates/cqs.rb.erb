# frozen_string_literal: true

module Command
end

module Query
end

Rails.autoloaders.main.push_dir(
  "#{Rails.root.join("app/commands/command")}", namespace: Command
)

Rails.autoloaders.main.push_dir(
  "#{Rails.root.join("app/queries/query")}", namespace: Query
)